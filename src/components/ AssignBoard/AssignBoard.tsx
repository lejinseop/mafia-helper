import type { FC } from 'react';
import { Grid } from '@mui/material';

interface AssignBoardProps {
  selectCountLimit: number;
}

const AssignBoard: FC = () => {
  return <Grid></Grid>;
};

export default AssignBoard;
